import { CustomMessageService } from './custom-message-service';

describe('CustomMessageService', () => {
  it('should create an instance', () => {
    expect(new CustomMessageService()).toBeTruthy();
  });
});
