import { AbstractCentralMessage } from './abstract-central-message';

describe('AbstractCentralMessage', () => {
  it('should create an instance', () => {
    expect(new AbstractCentralMessage()).toBeTruthy();
  });
});
